const a = new ArrayBuffer(4)

const array8 = new Uint8Array(a)
const array16 = new Uint16Array(a)
const array32 = new Uint32Array(a)

const uInt = new  Uint8Array(1.9 * 1024 * 1024 * 1024)

for (let i = 0; i < buffer.length; i++) {
   uInt
}
